@using Microsoft.AspNetCore.Mvc.Rendering
@model TaskEditViewModel
@{
    ViewData["Title"] = "Edit task";
    var taskId = (int)(ViewData["TaskId"] ?? 0);
    var users = ViewData["Users"] as SelectList;
    var statuses = ViewData["Statuses"] as SelectList;
}

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Edit task</h4>
        <form method="post" class="mt-3">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="ListId" />

            <div class="mb-3">
                <label asp-for="TaskName" class="form-label">TaskName</label>
                <input asp-for="TaskName" class="form-control" />
                <span asp-validation-for="TaskName" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="TaskDescription" class="form-label">TaskDescription</label>
                <textarea asp-for="TaskDescription" class="form-control" rows="4"></textarea>
                <span asp-validation-for="TaskDescription" class="text-danger"></span>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="TaskStartDate" class="form-label">TaskStartDate</label>
                    <input asp-for="TaskStartDate" class="form-control" />
                    <span asp-validation-for="TaskStartDate" class="text-danger"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="TaskFinishDate" class="form-label">TaskFinishDate</label>
                    <input asp-for="TaskFinishDate" class="form-control" />
                    <span asp-validation-for="TaskFinishDate" class="text-danger"></span>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="StatusId" class="form-label">StatusId</label>
                    <select asp-for="StatusId" class="form-select" asp-items="statuses"></select>
                    <span asp-validation-for="StatusId" class="text-danger"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="AssigndUserId" class="form-label">AssigndUserId</label>
                    <select asp-for="AssigndUserId" class="form-select" asp-items="users"></select>
                    <span asp-validation-for="AssigndUserId" class="text-danger"></span>
                </div>
            </div>

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="d-flex gap-2">
                <button class="btn btn-primary" type="submit">Save</button>
                <a class="btn btn-outline-secondary" asp-action="Details" asp-route-id="@taskId">Back</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
