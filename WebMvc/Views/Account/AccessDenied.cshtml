@{
    ViewData["Title"] = "Access denied";
}

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Access denied</h4>
        <p class="text-muted mb-0">You do not have permission to access this resource.</p>
    </div>
</div>
